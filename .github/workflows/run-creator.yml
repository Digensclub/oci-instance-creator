name: OCI Auto Instance Creator
on:
  schedule:
    - cron: '*/10 * * * *' # Triggers every 10 minutes
  workflow_dispatch:      # Allows manual trigger

jobs:
  launch-instance:
    runs-on: ubuntu-latest
    timeout-minutes: 10 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Create API Key File
        run: |
          echo "${{ secrets.OCI_KEY_FILE }}" > oci.key
          chmod 600 oci.key

      - name: Run Script
        env:
          OCI_USER_ID: ${{ secrets.OCI_USER_ID }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY_ID }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_IMAGE_ID: ${{ secrets.IMAGE_ID }}
          OCI_SUBNET_ID: ${{ secrets.SUBNET_ID }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          CPUS: ${{ secrets.CPUS }}
          VOLUME_SIZE: ${{ secrets.VOLUME_SIZE }}
          KEY_PATH: ./oci.key
        run: |
          go build -o creator *.go
          
          # Run exactly 5 times
          for i in {1..5}
          do
            echo "--- GitHub Action Attempt $i of 5 ---"
            
            # Start the creator.
            # 'timeout 30s' stops the Go tool's internal loop after 30 seconds.
            # '&& exit 0' stops everything if the VM is actually created.
            timeout 30s ./creator && exit 0 || echo "Attempt $i finished (no capacity or timeout). Retrying in 60s..."
            
            # Wait 60 seconds before the next attempt, but skip the wait after the last one
            if [ $i -lt 5 ]; then 
              sleep 60
            fi
          done
          
          echo "Reached 5 attempts limit. Workflow closing. See you in 10 minutes!"